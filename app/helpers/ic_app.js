const NAME = "localhost:4000",
  DOMAIN = `http://${NAME}`,
  SITE = `${DOMAIN}/`,
  COINS = `${SITE}coins`;
  GETOBJECTS = `${SITE}{OBJECTS}`;
  GETOBJECTS = `${SITE}{OBJECT}`;

export default {
  NAME,
  DOMAIN,
  SITE,
  COINS,
};
