//const NAME = "localhost:4000",
const NAME = "prevostocp.github.io/pageSPA/",
  DOMAIN = `https://${NAME}`,
  SITE = `${DOMAIN}/`,
  COINS = `${SITE}coins`;
//ADD_COIN = `${SITE}coins`;

export default {
  NAME,
  DOMAIN,
  SITE,
  COINS,
};
